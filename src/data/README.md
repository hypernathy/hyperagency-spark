# ⚠️ src/data — MIRROR (ne pas éditer le contenu quiz ici)

**Source canonique du contenu quiz = repo [`hyperyou-systems`](https://github.com/hypernathy/hyperyou-systems)** (décision `CONNEXA-HQ/_SYSTEM/DECISIONS.md` **D-014a**, 2026-06-09).

Les parcours **NeurYou** (`quizData*`) · **CupidYou** (`cupido*`) · **HorsYou** (`cavallo*`) existent
aussi, et **font foi**, dans `hyperyou-systems`. Ici, dans SPARK (`hyperagency-spark`), ce sont des
**copies-miroir**.

## Règle
- ❌ **Ne pas modifier** le contenu des quiz (questions, archétypes, overlays) directement ici.
- ✅ Toute évolution du contenu → la faire dans **`hyperyou-systems`**, puis **synchroniser** ici.
- ✅ Le code spécifique à SPARK (comptes, dashboard, spark-chat) reste, lui, propre à ce repo.

Pourquoi : éviter que les deux repos re-divergent (ils sont sur 2 projets Supabase différents).
Voir aussi `../../CLAUDE.md` § « Réconciliation cross-repo ».
